# Example environment configuration for Size Core widget
# Copy this file to `.env` (for local development) or `.env.production` (for builds)
# and adjust the values for each environment.

# URL of the Miqyas frontend flow embedded in the iframe
VITE_MIQYAS_FRONTEND_URL=

# Socket.IO backend endpoint used for desktop flows
VITE_MIQYAS_BACKEND_WS_URL=

# Optional tracking endpoints (leave blank to disable)
VITE_SIZE_CORE_TRACK_CLICK_ENDPOINT=
VITE_SIZE_CORE_TRACK_RETURN_ENDPOINT=

# Optional custom Socket.IO path override
VITE_SIZE_CORE_SOCKET_PATH=/socket.io
